# Javascript para React

Para esta seccion se recomienda tener la extension **Live Server** instalada

### Loopback y Localhost

* `127.0.0.1:5500` es una direccion de **Loopback**, es una direccion IP que corresponde a un servidor `localhost`
* Tambien podemos escribir `localhost:5500` para acortar el mismo
